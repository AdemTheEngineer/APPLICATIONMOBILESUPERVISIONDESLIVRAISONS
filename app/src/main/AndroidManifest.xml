<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ───────────────────────────────────────────────────── -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.APPLICATIONMOBILESUPERVISIONDESLIVRAISONS"
        tools:targetApi="31">

        <!-- ── Launcher ──────────────────────────────────────────────────── -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Controleur Activities ─────────────────────────────────────── -->
        <activity
            android:name=".activities.controleur.ControleurActivity"
            android:label="Espace Contrôleur"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.controleur.DetailLivraisonActivity"
            android:label="Détails Livraison"
            android:exported="false"
            android:parentActivityName=".activities.controleur.ControleurActivity" />

        <activity
            android:name=".activities.controleur.SearchActivity"
            android:label="Recherche"
            android:exported="false"
            android:parentActivityName=".activities.controleur.ControleurActivity" />

        <activity
            android:name=".activities.controleur.HubActivity"
            android:label="Comm Hub"
            android:exported="false"
            android:parentActivityName=".activities.controleur.ControleurActivity" />

        <!-- ── Livreur Activities ─────────────────────────────────────────── -->
        <activity
            android:name=".activities.livreur.LivreurActivity"
            android:label="Espace Livreur"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.livreur.EmergencyAlertsActivity"
            android:label="Emergency Alerts"
            android:exported="false"
            android:parentActivityName=".activities.livreur.LivreurActivity" />

        <activity
            android:name=".activities.livreur.DriverHubActivity"
            android:label="Comm Hub"
            android:exported="false"
            android:parentActivityName=".activities.livreur.LivreurActivity" />

    </application>

</manifest>